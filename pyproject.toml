[build-system]
requires = ["hatchling>=1.21.0"]
build-backend = "hatchling.build"

[project]
name = "noesis"
version = "1.0.0"
description = "Noēsis — a framework for intuition-guided agentic reasoning."
readme = { file = "README.md", content-type = "text/markdown" }
license = { file = "LICENSE" }
requires-python = ">=3.11"
authors = [{ name = "Sara Loera" }]
keywords = ["agents", "ai", "intuition", "reasoning", "langgraph", "crewai"]
classifiers = [
  "Development Status :: 5 - Production/Stable",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.12",
  "License :: OSI Approved :: Apache Software License",
  "Operating System :: OS Independent",
]
# Runtime dependencies
dependencies = [
  "ruamel.yaml>=0.18",
]

[project.optional-dependencies]
migrate = ["libcst>=1.5.0"]
ui = [
  "rich>=14.2.0",
  "rich-argparse>=1.7.1",
  "textual>=0.79.0",
  "streamlit>=1.51.0",
]
dev = [
  "pytest>=7.4",
  "jsonschema>=4.21",
  "ruff>=0.5",
  "import-linter>=2.0",
  "build>=1.0.0",
  "ruamel.yaml>=0.18",
]
langgraph = [
  "langgraph>=0.0.45",
  "langchain>=0.1.0",
]

[dependency-groups]
dev = [
  "pytest>=7.4",
  "jsonschema>=4.21",
  "ruff>=0.5",
  "import-linter>=2.0",
  "build>=1.0.0",
  "ruamel.yaml>=0.18",
  "libcst>=1.8.6",
]
ui = [
  "rich>=14.2.0",
  "rich-argparse>=1.7.1",
  "textual>=0.79.0",
  "streamlit>=1.51.0",
]
migrate = ["libcst>=1.5.0"]

[project.scripts]
noesis = "noesis.cli.__main__:main"


[project.urls]
Homepage = "https://github.com/saraeloop/noesis"
Repository = "https://github.com/saraeloop/noesis"
Documentation = "https://deepwiki.com/saraeloop/noesis"

# Control build contents 
[tool.hatch.build]
exclude = ["tests", "tests/**", "examples", "examples/**"]
# Include only the package and selected top-level files
include = [
  "noesis/**",
  "LICENSE",
  "README.md",
  "pyproject.toml",
]
[tool.hatch.build.targets.sdist]
include = [
  "noesis/**",
  "docs/**",
  "flows/**",
  "README.md",
  "LICENSE",
  "pyproject.toml",
]

[tool.importlinter]
root_package = "noesis"

[[tool.importlinter.contracts]]
name = "Runtime must not import adapters"
type = "forbidden"
source_modules = [
  "noesis.runtime",
]
forbidden_modules = [
  "noesis.adapters",
]

[[tool.importlinter.contracts]]
name = "Adapters must not import runtime internals"
type = "forbidden"
source_modules = [
  "noesis.adapters",
]
forbidden_modules = [
  "noesis.runtime",
]

[[tool.importlinter.contracts]]
name = "Runtime must not import CLI"
type = "forbidden"
source_modules = [
  "noesis.runtime",
]
forbidden_modules = [
  "noesis.cli",
]

[[tool.importlinter.contracts]]
name = "Domain stays decoupled"
type = "forbidden"
source_modules = [
  "noesis.domain",
]
forbidden_modules = [
  "noesis.runtime",
  "noesis.cli",
]

[[tool.importlinter.contracts]]
name = "Usecases do not import adapters"
type = "forbidden"
source_modules = [
  "noesis.usecases",
]
forbidden_modules = [
  "noesis.adapters",
]

[[tool.importlinter.contracts]]
name = "Usecases do not import CLI"
type = "forbidden"
source_modules = [
  "noesis.usecases",
]
forbidden_modules = [
  "noesis.cli",
]
