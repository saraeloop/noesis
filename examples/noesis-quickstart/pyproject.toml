[project]
name = "noesis-quickstart"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langgraph>=1.0.5",
    "noesis",
    "openai>=2.14.0",
    "jsonschema>=4.21.0",
]

[tool.uv.sources]
noesis = { path = "../..", editable = true }
