---
title: "Guarded LangGraph (Deprecated)"
description: "This tutorial has been split into two single-purpose tutorials."
slug: guarded-langgraph-agent
---

This tutorial was split into two single‑purpose tutorials for clarity:

1) **LangGraph Episode** — trace the cognitive loop with `ns.solve(...)`
2) **Governed Side Effects** — enforce `action_candidate → governance → act` with `ns.governed_act(...)`

Run:

```bash
uv run python -m tutorials.langgraph_episode
uv run python -m tutorials.governed_side_effects
```

Docs:
- `docs/tutorials/langgraph-episode.mdx`
- `docs/tutorials/governed-side-effects.mdx`
